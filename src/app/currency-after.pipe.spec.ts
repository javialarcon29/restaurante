import { CurrencyAfterPipe } from './currency-after.pipe';

describe('CurrencyAfterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyAfterPipe();
    expect(pipe).toBeTruthy();
  });
});
